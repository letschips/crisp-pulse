# Changelog - Crisp Pulse

## [1.7.0] - 2026-09-14

### Added
- 新增词数来源：系统目录、大段捕获估算、其他未确认来源与历史未分类；支持主动排除后续 Sidecar 日志/备份/清单，不改历史统计。
- 跨期行动：新增行动、从上一等长周期带入未完成项、完成/延期/取消状态与结果笔记关联；重复导入去重，保留来源周期。
- 洞见证据：搜索本期笔记，预览并选择原文摘录，保存前核对原文；报告包含来源、摘录时间与快照。重命名更新工作链接，保留原始路径。

### Fixed
- 同日重建文件累加已观察字数，避免覆盖原记录。
- 重新纳入系统目录前建立快照，避免补计排除期间的编辑。
- 摘录核对兼容 CRLF，重复摘录去重；中文输入与文本选区在刷新时保持。
- 复盘日期在打开后固定，避免跨午夜时把正在编辑的草稿切换到另一周期。

---

## [1.6.0] - 2026-09-13

### Added
- 知识复盘支持自选日期（最多 371 天）、最近 7/30/90 天和前后周期导航；显示前一个等长周期的六项指标与数据覆盖。
- 目录与笔记钻取：按完整目录边界过滤，按新增词数、改写估算、任务或记录天数排序，分批查看全部记录并打开原笔记。
- 复盘报告：按日期区间和质量范围保存进展、洞见、阻塞、下一步草稿；复制 Markdown 或归档，既有报告不覆盖。

### Fixed
- 复盘不再显示无效的全局时间选择器，采用独立且实际生效的日期控件；滚动七天明确标为“最近 7 天”。
- 空记录不再生成知识流向均衡或知识质量结论；目录统计明确标为文件×日期记录数。
- 输入刷新保留草稿、焦点与选区，中文输入法组字期间延迟重绘。
- 全量 CSV 按钮明确导出范围，避免误认为随页面日期或质量筛选。

---

## [1.5.3] - 2026-09-11

### Improved
- **看板头部动态脉冲图标**：将看板视图头部原静态闪电图标升级为波浪动态 SVG 图标（blocks wave），自适应深浅色主题。

---

## [1.5.2] - 2026-09-09

### Improved
- **图表适配与体验优化**：优化窄面板响应式重绘逻辑，完善缺失日期与零值区隔显示。
- **稳定性增强**：优化键盘导航与日明细面板交互响应。

---

## [1.5.1] - 2026-09-09

### Fixed
- **时长汇总精度**：改为先累计原始分钟、最后统一格式化，避免逐日四舍五入丢掉零点几分钟的小值；看板与周复盘口径保持一致。
- **目录占比合计偏差**：改用最大余数分配整数百分比，消除 99% / 101% 这类合计不为 100% 的情况；有文件记录时合计为 100%。该占比表示文件与日期记录次数，不代表真实精力或工时。
- **异常日期校验**：拒绝 `2026-02-31` 这类无法往返转换的日历日期，统一覆盖时间过滤与质量筛选入口，且不删除原始异常记录。
- **新建与修改竞态**：新建与修改共用单文件队列，防止新建读操作与紧随的修改交叉推进快照；重复创建事件不再重复计入已有快照。
- **导入捕获折扣**：新建文件的 500 词以上初始内容纳入既有捕获折扣逻辑，不再绕过。
- **周复盘时间范围**：命令与视图统一为包含今天的最近 7 天；复制周报沿用同一质量筛选，不再带入全部历史记录。
- **热力图宽度与月份坐标**：月份与日期共用 53 列网格，单元格保持正方形、星期标签随行高对齐；宽屏铺满内容宽度，窄屏在卡片内部横向滚动，并为边缘焦点轮廓保留内边距。

---

## [1.5.0] - 2026-09-09

### Added
- **数据分析图表视图 (Analytics)**：新增「数据分析」独立视图，支持原生 SVG 绘制的每日贡献柱状图、写作变化折线图（新增/删除/改写）与时间投入折线图（交互活跃/专注时长）。
- **多周期切换**：支持 7 天、30 天与 90 天分析周期快速切换，并沿用既有数据质量筛选口径。
- **交互与可访问性**：支持悬停与键盘导航（左右方向键逐日查看、Home/End 跳转、Enter 打开日明细），自适应宽窄面板。

---

## [1.4.1] - 2026-09-09

### Fixed
- **数据防损与自愈保护**：移除启动阶段对单日记录超过 10 万字时的推测性粗暴删减，严格保留文件明细与原始未校验质量标记（`legacyUnverified`）。
- **备份写入安全与去重**：备份操作改为串行队列执行，加入 6 位自增序列号与已有文件防重检测，杜绝同秒内多重备份相互覆盖。
- **备份轮换范围约束**：采用严格正则匹配插件命名规范的备份文件，避免轮换清理时误删目录内的其他外部 JSON 数据。
- **生命周期安全卸载**：在文件读盘及队列处理全链路增加运行状态检查，防止插件卸载或重载后悬挂的异步任务写入过期快照与日统计。
- **重命名队列释放**：锁定入队原始路径作为清理键，修复异步读取期间文件重命名导致内部队列残留 Promise 引起的内存泄漏。
- **热力图全景稳定与区间聚焦**：时间筛选保持 53 周完整年景展示，统计区间外日期淡化呈现作为参考，既避免短周期筛选时大面积空白破版，又兼顾 KPI 范围聚焦与年度工作连续性感知；修复范围筛选时属性引用导致的空状态误判。
- **DOM 重绘后焦点与滚动保护**：修复日期选择后尝试聚焦已销毁 DOM 的问题，重绘后平滑恢复单元格焦点与横向滚动位置。
- **窄屏布局适配**：优化顶部操作区 Flex 换行与宽度约束，修复 420px 侧边分栏下的 17px 溢出问题。

---

## [1.4.0] - 2026-09-08

### Added
- **Crisp 授权与设备激活体系**：引入 Ed25519 离线公钥签名校验与轻量设备激活机制，支持同设备多插件自动继承授权。
- **关于 Crisp Pulse 卡片**：在插件设置面板底部新增 Crisp 品牌信息与作者主页（小红书 letschips）入口。
- **UI 图标升级**：贡献得分构成拆解标题采用标准矢量电脑 SVG 图标，提升界面质感。

### Improved
- **指标可靠度与自愈机制**：增强启动与运行时自愈，彻底隔离历史未经实测的异常数据，保证热力图与统计指标客观真实。
- **单元与回归测试**：扩充至 52 项自动化测试，100% 覆盖授权验证、热力图计算、任务生命周期、周复盘生成及生态联动。

---

## [1.3.0] - 2026-09-08

### Added
- **Crisp Focus 生态联动**：自动探测 Crisp Focus 番茄钟专注状态，专注时长无缝累计并折算贡献得分，状态栏呼吸脉冲动效。
- **ANKS 知识库周报归档**：周复盘支持一键生成规范 ANKS 结构化笔记并归档到知识库。

---

## [1.2.0] - 2026-09-07

### Added
- **有效编辑判定算法**：引入行差异指纹分析，精准捕捉等长重构与深度润色行为。
- **任务防刷分生命周期**：已完成任务指纹集合，杜绝复选框重复勾选虚增得分。
- **工作周复盘面板**：多维时间范围选择与精力流向分布比例可视化。

---

## [1.1.0] - 2026-09-06

### Added
- 知识库路径黑白名单与 ANKS 预设过滤。
- CSV 与 JSON 全量数据安全导出。

---

## [1.0.0] - 2026-09-01

### Added
- 初始版本：基于本地第一原则的知识工作热力图与贡献度统计。
- 状态栏实时活跃状态指示。
